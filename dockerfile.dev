FROM node:20-alpine

ARG PORT
ENV PORT=$PORT

ARG NODE_ENV
ENV NODE_ENV=$NODE_ENV

ARG OPENAI_API_KEY
ENV OPENAI_API_KEY=$OPENAI_API_KEY

ARG API_SECRET
ENV API_SECRET=$API_SECRET

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE ${PORT}

CMD ["npm", "run", "dev"] 